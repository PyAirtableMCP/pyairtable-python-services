# Airtable Gateway Configuration

# Service
SERVICE_NAME=airtable-gateway
SERVICE_VERSION=1.0.0
HOST=0.0.0.0
PORT=8002

# Airtable - SECURITY: Must be set via environment variable
AIRTABLE_TOKEN=${AIRTABLE_TOKEN}
AIRTABLE_RATE_LIMIT=5
AIRTABLE_TIMEOUT=30

# Redis
REDIS_URL=${REDIS_URL:-redis://localhost:6379/0}
CACHE_TTL=3600

# Security - SECURITY: Must be set via environment variable
INTERNAL_API_KEY=${INTERNAL_API_KEY}

# Logging
LOG_LEVEL=INFO

# CORS
CORS_ORIGINS=["*"]